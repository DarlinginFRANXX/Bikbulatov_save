<Page x:Class="Galiev_Глазки_save.AddPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Galiev_Глазки_save"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Агент" FontFamily="Century Gothic">

    <Page.Resources>
        <local:StringToImageConverter x:Key="StringToImageConverter" />
    </Page.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая колонка -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Наименование:" VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Title}" Margin="5" MaxLength="100"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Адрес:" VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Address}" Margin="5"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="ФИО директора:" VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding DirectorName}" Margin="5"/>

        <!-- Логотип с рамкой -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Логотип:" VerticalAlignment="Top" Margin="5"/>
        <Border Grid.Row="3" Grid.Column="1" BorderThickness="1" BorderBrush="Gray" 
                CornerRadius="3" Margin="5" Width="120" Height="120"
                HorizontalAlignment="Left" VerticalAlignment="Top">
            <Image Name="LogoImage" Stretch="Uniform" 
                   VerticalAlignment="Center" HorizontalAlignment="Center"
                   MaxWidth="110" MaxHeight="110"
                   Source="{Binding Logo, Converter={StaticResource StringToImageConverter}}"/>
        </Border>

        <Button Grid.Row="4" Grid.Column="1" Content="Изменить логотип" Name="ChangePictureBtn" 
                Width="150" Height="30" Click="ChangePictureBtn_Click" Margin="5"/>

        <!-- Правая колонка -->
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Тип агента:" VerticalAlignment="Center" Margin="5"/>
        <ComboBox Grid.Row="5" Grid.Column="1" Name="ComboType" Margin="5" SelectedIndex="0">
            <ComboBoxItem>ЗАО</ComboBoxItem>
            <ComboBoxItem>МКК</ComboBoxItem>
            <ComboBoxItem>МФО</ComboBoxItem>
            <ComboBoxItem>ОАО</ComboBoxItem>
            <ComboBoxItem>ООО</ComboBoxItem>
            <ComboBoxItem>ПАО</ComboBoxItem>
        </ComboBox>

        <TextBlock Grid.Row="6" Grid.Column="0" Text="Приоритет:" VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Priority}" Margin="5"/>

        <TextBlock Grid.Row="7" Grid.Column="0" Text="ИНН:" VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding INN}" Margin="5"/>

        <TextBlock Grid.Row="8" Grid.Column="0" Text="КПП:" VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding KPP}" Margin="5"/>

        <TextBlock Grid.Row="9" Grid.Column="0" Text="Email:" VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="9" Grid.Column="1" Text="{Binding Email}" Margin="5"/>

        <TextBlock Grid.Row="10" Grid.Column="0" Text="Телефон:" VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="10" Grid.Column="1" Text="{Binding Phone}" Margin="5"/>

        <!-- История продаж -->
        <TextBlock Grid.Row="11" Grid.ColumnSpan="2" Text="История продаж" FontSize="18" Margin="10,5"/>
        <StackPanel Grid.Row="12" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="10,5">
            <TextBlock Text="Продукт:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox Name="ProductCombo" Width="400" IsEditable="True" DisplayMemberPath="Title" Margin="5"/>
            <TextBlock Text="Кол-во:" VerticalAlignment="Center" Margin="5"/>
            <TextBox Name="ProductCountBox" Width="60" Margin="5"/>
            <TextBlock Text="Дата:" VerticalAlignment="Center" Margin="5"/>
            <DatePicker Name="SaleDatePicker" Width="150" Margin="5"/>
            <Button Content="Добавить" Name="AddSaleBtn" Click="AddSaleBtn_Click" Width="100" Margin="5"/>
        </StackPanel>

        <ListView Name="SalesListView" Grid.Row="13" Grid.ColumnSpan="2" Margin="10" MaxHeight="150">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Product.Title}" Width="400"/>
                        <TextBlock Text="{Binding ProductCount}" Width="60"/>
                        <TextBlock Text="{Binding SaleDate, StringFormat=d}" Width="100"/>
                        <Button Content="Удалить" Tag="{Binding}" Click="DeleteSaleBtn_Click" Width="85"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Кнопки -->
        <StackPanel Grid.Row="14" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Сохранить" Name="SaveBtn" Click="SaveBtn_Click" Width="100" Margin="5"/>
            <Button Content="Удалить" Name="DeleteBtn" Click="DeleteBtn_Click" Width="100" Margin="5"/>
            <Button Content="Отмена" Name="CancelBtn" Click="CancelBtn_Click" Width="100" Margin="5"/>
        </StackPanel>
    </Grid>
</Page>